%body
  .page
    .header
      .container-header
        %img.header-logo{src:'https://www.kabisa.nl/images/kabisa-logo-d9ace243.png'}
    .body
      .container-left
        %div#new-transaction
          = render 'dashboard/new-transaction'
        .dropdown
          = render 'dashboard/drop-down'
        .statistics
          = render 'dashboard/statistics'
      .container-center
        = render 'dashboard/transaction'
        .error-container
          - if @transaction.errors.any?
            %h2.error-title
              = pluralize(@transaction.errors.count, 'error')
              prohibited this transaction from being created:
            .error-body
              %ul
                = @transaction.errors.full_messages.each do |msg|
                  %li
                    = msg
        #results
          = render 'dashboard/timeline'
      .container-right
        .kudo-clicker
          -#= render 'dashboard/kudos-counter'
