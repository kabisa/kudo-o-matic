.statistics-container
  %h1
    Transactions
    .all
      .value
        = Transaction.all.count(:id)
      .label
        this year
    .month
      .value
        = Transaction.where(created_at: (Time.now.beginning_of_month..Time.now.end_of_month)).count(:id)
      .label
        this month
    .week
      .value
        = Transaction.where(created_at: (Time.now.beginning_of_week(start_day = :monday)..Time.now.end_of_week())).count(:id)
      .label
        this week
  -#%h1.kudos
  -#  Kudo's
  -#  .month
  -#    .value
  -#      = Transaction.where(created_at: (Time.now.beginning_of_month..Time.now.end_of_month)).where(balance: Balance.current).sum(:amount)
  -#    .label
  -#      this month
  -#  .week
  -#    .value
  -#      = Transaction.where(created_at: (Time.now.beginning_of_week(start_day = :monday)..Time.now.end_of_week())).where(balance: Balance.current).sum(:amount)
  -#    .label
  -#      this week